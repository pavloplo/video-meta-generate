apiVersion: apps/v1
kind: Deployment
metadata:
  name: video-meta-app
spec:
  template:
    spec:
      containers:
        - name: app
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"